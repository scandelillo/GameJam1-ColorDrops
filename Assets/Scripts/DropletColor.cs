// Los tres colores del juego.
// El valor numérico coincide con el índice del slice en el material (0 = rojo, 1 = amarillo, 2 = azul),
// para poder convertir enum <-> índice de forma directa con un simple (int).
public enum DropletColor
{
    Rojo = 0,
    Amarillo = 1,
    Azul = 2,
}
